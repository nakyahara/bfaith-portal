'use strict';
const escape=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function render(run){
 require('./kw-discovery.cjs').validateDiscovery(run);
 const e=escape;return '<!doctype html><html lang="ja"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>判定待ちのKW案</title><style>body{font-family:system-ui;background:#f4f7f8;color:#193345;max-width:1000px;margin:30px auto;padding:0 18px}article{background:white;border:1px solid #dae4e9;padding:18px;margin:16px 0;border-radius:10px}h2{color:#126b82}p,li{line-height:1.7}.notice{background:#fff4cc;padding:12px}a{color:#126b82}</style><h1>判定待ちのKW案</h1><p>'+e(run.day)+' ／ 今回 '+run.submitted_count+'案（新規 '+run.new_count+'案）</p><p>確認用プレビューです。判断の保存は本番ポータルで行います。</p><p>Keepaの'+(run.coverage.input_this_run??run.coverage.examined_this_run)+'商品を入力（対応確認済み '+run.coverage.examined_this_run+'商品）。今回の巡回：'+run.coverage.seen_in_cycle+' / '+run.coverage.unique_products+'商品。参考にした判断：'+run.learning_audit.judgement_count+'件。</p>'+(run.filter_audit?'<p>内部で作った '+run.filter_audit.generated+'案から選別し、判定候補 '+run.items.length+'案。対象外・重複・調査待ちはこの一覧に含めません。</p>':'')+run.warnings.map(w=>'<p class="notice">'+e(w)+'</p>').join('')+run.items.map(i=>'<article><h2>'+e(i.kw)+'</h2><p>'+e(i.idea)+'</p><p>'+e(i.screening?.opportunity||i.reason)+'</p><details><summary>発想の元になったKeepa商品・未確認事項</summary><ul>'+i.evidence.map(o=>'<li><a href="'+e(o.url)+'" target="_blank" rel="noopener">'+e(o.title_excerpt)+'</a><br>保存時の参考価格 '+e(o.recorded_price==null?'不明':o.recorded_price+'円')+' ／ 保存時の購入表示 '+e(o.recorded_monthly_units==null?'不明':o.recorded_monthly_units+'以上')+'（現在の値は未確認）<br>保存日時 '+e(o.recorded_at||'不明')+'</li>').join('')+'</ul><p>'+i.unknowns.map(e).join('。')+'</p></details></article>').join('')+'</html>';
}
module.exports={render};
